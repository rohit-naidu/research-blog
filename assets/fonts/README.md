# Fonts

This folder is reserved for self-hosted serif fonts.

The stylesheet currently uses a safe serif stack that starts with `ET Book`
and falls back to macOS and browser-default academic serif fonts. If you later
add ET Book `.woff2` files here, we can wire them into `assets/css/style.css`
with `@font-face`.
